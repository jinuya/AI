# AI

AI 실험 및 작업용 저장소.

## 이 저장소의 성격

여러 실험을 담는 스크래치 저장소입니다. 고정된 아키텍처나 빌드 시스템이 아직 없으므로,
작업 전에 **실제 디렉터리 구조를 먼저 확인**하세요. 이 문서에 적히지 않은 구조를 가정하지 마세요.

새 실험은 최상위에 디렉터리 하나로 분리합니다. 실험끼리 코드를 공유하지 않는 것이 기본이며,
공유가 필요해지면 그때 공용 모듈로 승격합니다.

## 규칙

- **비밀값은 커밋하지 않습니다.** API 키, 토큰, 자격 증명은 `.env`에 두고 `.env.example`에 키 이름만 남깁니다.
  `.env`는 `.gitignore`에 포함되어 있습니다.
- **대용량 산출물은 커밋하지 않습니다.** 모델 가중치, 체크포인트, 데이터셋은 저장소 밖에 두거나
  다운로드 스크립트로 재현 가능하게 합니다. `.gitignore`가 흔한 확장자를 이미 막고 있습니다.
- **작업은 브랜치에서.** 클라우드 세션도 새 브랜치로 push하므로 `main`은 리뷰를 거친 것만 들어옵니다.

## 아직 정해지지 않은 것

아래는 첫 실험을 추가할 때 확정하고 이 문서를 갱신하세요.

- 주 언어 및 런타임 버전 (Python / Node 등)
- 패키지 매니저와 의존성 파일 (`uv` / `poetry` / `pnpm` 등)
- 테스트 러너와 실행 명령
- 린터 / 포매터

## 클라우드 세션

이 저장소는 Claude Code 클라우드 세션에서도 사용합니다. 클라우드 VM은 저장소를 fresh clone하므로
**커밋된 것만** 보입니다. 로컬에만 설치·설정한 도구는 넘어가지 않습니다.
클라우드에서 필요한 의존성은 환경의 셋업 스크립트에 넣거나 저장소에 커밋하세요.
